"""Retrieval over the Wikivoyage corpus: dense, sparse, and the fusion of both.

`hybrid_search` is travel-assist's frozen retrieval entrypoint — called directly
by day 1's pipeline, and available unmodified to any downstream consumer.
"""
