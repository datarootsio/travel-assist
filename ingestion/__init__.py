"""Building the index. INSTRUCTOR ONLY — participants read this, they never run it.

Ingestion is deliberately off the student critical path: nineteen people each
embedding a hundred thousand chunks is too slow and too expensive to be a
lesson. The time saved goes into hybrid search, conversation management and the
routing pipeline instead. See `ingestion/README.md`.
"""
